version: '3.8'

services:
  agentgate:
    build: .
    container_name: agentgate-firewall
    ports:
      - "8765:8765"
    environment:
      - TELEGRAM_BOT_TOKEN=${TELEGRAM_BOT_TOKEN:-}
      - TELEGRAM_CHAT_ID=${TELEGRAM_CHAT_ID:-}
    restart: unless-stopped
